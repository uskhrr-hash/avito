"""Разовый импорт input/autoload_working.xlsx → SQLite listings.

OBSOLETE: Excel battle path removed. Keep only if you still have a backup xlsx
under backup/xlsx-orphans-*/. Do not use in daily/runtime.
"""
from __future__ import annotations

raise SystemExit(
    "migrate_working_to_listings.py is obsolete (Excel removed). "
    "Restore from backup/xlsx-orphans if you truly need a one-off import."
)
