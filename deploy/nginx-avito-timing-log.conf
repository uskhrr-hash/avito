# Timing for avito.shinaufa.ru access logs (hang diagnosis).
log_format avito_timing '$remote_addr - $remote_user [$time_local] '
                        '"$request" $status $body_bytes_sent '
                        'rt=$request_time urt=$upstream_response_time '
                        '"$http_referer" "$http_user_agent"';
